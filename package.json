{
  "name": "victxr-lev",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "visual": "node tests/visual.mjs",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "astro": "^7.1.6",
    "gsap": "^3.15.0"
  },
  "devDependencies": {
    "playwright": "^1.60.0",
    "wrangler": "^4.115.0"
  }
}
