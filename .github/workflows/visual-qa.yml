name: Visual QA

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  screenshots:
    runs-on: ubuntu-latest
    timeout-minutes: 24

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 22
          package-manager-cache: false

      - name: Install dependencies
        run: npm ci --no-audit --no-fund

      - name: Install browsers
        run: npx playwright install --with-deps chromium firefox webkit

      - name: Build
        run: npm run build

      - name: Type and content checks
        run: npm run check

      - name: Static production audit
        run: npm run audit:static

      - name: Start preview
        run: |
          npm run preview -- --host 127.0.0.1 > /tmp/victxr-preview.log 2>&1 &
          for i in {1..30}; do
            if curl -fsS http://127.0.0.1:4321 >/dev/null; then
              exit 0
            fi
            sleep 1
          done
          cat /tmp/victxr-preview.log
          exit 1

      - name: Capture visual states and final gates
        run: npm run visual

      - name: Upload screenshots
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: visual-qa-${{ github.sha }}
          path: artifacts/visual
          if-no-files-found: warn
          retention-days: 14
